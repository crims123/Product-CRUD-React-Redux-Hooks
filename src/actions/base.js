export const request = (type, payload) => {
  return {
    type,
    payload,
  };
};

export const success = (type, payload) => {
  return {
    type,
    payload,
  };
};

export const error = (type, payload) => {
  return {
    type,
    payload,
  };
};
